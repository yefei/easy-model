'use strict';

const OPTIONS = Symbol('Model#options');
const QUERY = Symbol('Model#query');
const UPDATE = Symbol('Model#update');
const MODEL = Symbol('Model#model');
const PKVAL = Symbol('Model#pkValue');

module.exports = {
  OPTIONS,
  QUERY,
  UPDATE,
  MODEL,
  PKVAL,
};
